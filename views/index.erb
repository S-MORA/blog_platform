<!-- carousel -->

<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
  </ol>
  <!-- carousel of latest posts  -->
  <div class="carousel-inner" role="listbox">
    <div class="carousel-item active">
      <img class="d-block position-left img-fluid" src="images/article-1.png" alt="First slide">
      <div class="hero-text-container">
        <div class="hero-text">
          Advanced Reproductive Technology is Here. But Who Decides Who Gets Access?
        </div>
      </div>

    </div>
    <div class="carousel-item">
      <img class="d-block position-left img-fluid" src="images/article-2.png" alt="Second slide">
      <div class="hero-text-container">
        <div class="hero-text">
          An exploration of Tycho’s New Album & His Design as ISO500
        </div>
      </div>
    </div>
    <div class="carousel-item">
      <img class="d-block position-left img-fluid" src="images/article-3.png" alt="Third slide">
      <div class="hero-text-container">
        <div class="hero-text">
          Best music videos of 2018 so far
        </div>
      </div>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
  </a>
</div>

<div class="parallax fade-in display-none">Latest</div>

<!-- main content  -->

<div class="grid">
  <% @posts.reverse_each do |post| %>
    <% author = post.user %>
      <% if post.id.to_i % 3 == 0 %>
        <div class="grid-item">
          <% elsif post.id.to_i % 2 == 0 %>
             <div class="grid-item grid-item--width2">
            <% else %>
              <div class="grid-item grid-item--width3">
            <%end%>

          <a href="/post/<%=post.id.to_i%>">
            <% if post.tag == 'science' %>
              <div class="contain-overlay overlay pink">
            <% elsif post.tag == 'music' %>
               <div class="contain-overlay overlay blue">
            <% else %>
               <div class="contain-overlay overlay yellow">
            <%end%>
                 <img src="<%=post.img%>" alt=""><br>
              </div>
           </a>

              <%t = post.updated_at%>
                <%m = t.strftime("%m")%>
                  <%d = t.strftime("%d")%>
                    <span class="sm-gray"><%=m%>.<%=d%>
                      <%= post.tag%>
                      <%= author.f_name.downcase%>
                      <%= author.l_name.downcase%></span><br>
                    <span class="md-black"><%=post.title%></span>
                  </div>
            
              <% end %>
            </div>

  <div class="parallax fade-in display-none">Latest</div>
